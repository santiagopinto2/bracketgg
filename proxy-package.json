{
  "name": "startgg-oauth-proxy",
  "version": "1.0.0",
  "description": "Simple proxy server for start.gg OAuth to avoid CORS issues",
  "main": "proxy-server.js",
  "scripts": {
    "start": "node proxy-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  }
}
